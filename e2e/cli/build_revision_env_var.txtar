env BUILD_NUMBER=build-12345
exec $OPACTL build --config config.d/bundle.yml --data-dir tmp --non-interactive
! stderr .
! stdout .

exec tar xf bundles/hello-world/bundle.tar.gz /.manifest
cmp .manifest exp/.manifest

-- config.d/bundle.yml --
bundles:
  hello-world:
    revision: opa.runtime().env.BUILD_NUMBER
    object_storage:
      filesystem:
        path: bundles/hello-world/bundle.tar.gz
-- exp/.manifest --
{"revision":"build-12345","roots":[""],"rego_version":0}
